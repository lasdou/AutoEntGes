{% extends 'base.html.twig' %}

{% block body %}
    <h1>Adresses list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Adresse_1</th>
                <th>Adresse_2</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for adresse in adresses %}
            <tr>
                <td><a href="{{ path('adresse_show', { 'id': adresse.id }) }}">{{ adresse.id }}</a></td>
                <td>{{ adresse.nom }}</td>
                <td>{{ adresse.adresse1 }}</td>
                <td>{{ adresse.adresse2 }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('adresse_show', { 'id': adresse.id }) }}" class="btn btn-secondary"><i class="fas fa-search"></i></a>
                        </li>
                        <li>
                            <a href="{{ path('adresse_edit', { 'id': adresse.id }) }}" class="btn btn-primary"><i class="fas fa-pencil"></i></a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adresse_new') }}" class="btn btn-success"><i class="fas fa-plus"></i>Créer un nouveau adresse</a>
        </li>
    </ul>
{% endblock %}
