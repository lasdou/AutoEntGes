{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pays list</h1>

     <table class="table table-striped table-bordered table-dark">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pay in pays %}
            <tr>
                <td><a href="{{ path('pays_show', { 'id': pay.id }) }}">{{ pay.id }}</a></td>
                <td>{{ pay.nom }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pays_show', { 'id': pay.id }) }}" class="btn btn-secondary"><i class="fas fa-eye"></i> Afficher</a>
                        </li>
                        <li>
                            <a href="{{ path('pays_edit', { 'id': pay.id }) }}" class="btn btn-primary"><i class="fas fa-edit"></i> Editer</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pays_new') }}" class="btn btn-success"><i class="fas fa-plus"> </i>Créer un nouveau  pays</a>
        </li>
    </ul>
{% endblock %}
