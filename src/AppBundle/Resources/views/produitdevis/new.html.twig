{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produitdevi creation</h1>

    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('produitdevis_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
