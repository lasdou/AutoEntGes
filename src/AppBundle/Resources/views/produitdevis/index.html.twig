{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produitdevis list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Code</th>
                <th>Prix_ht</th>
                <th>Quantite</th>
                <th>Remise_ht</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produitDevi in produitDevis %}
            <tr>
                <td><a href="{{ path('produitdevis_show', { 'id': produitDevi.id }) }}">{{ produitDevi.id }}</a></td>
                <td>{{ produitDevi.libelle }}</td>
                <td>{{ produitDevi.code }}</td>
                <td>{{ produitDevi.prixht }}</td>
                <td>{{ produitDevi.quantite }}</td>
                <td>{{ produitDevi.remiseht }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('produitdevis_show', { 'id': produitDevi.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('produitdevis_edit', { 'id': produitDevi.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produitdevis_new') }}">Créer un nouveauproduitDevi</a>
        </li>
    </ul>
{% endblock %}
