{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produitdevi</h1>

     <table class="table table-striped table-bordered table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produitDevi.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ produitDevi.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ produitDevi.code }}</td>
            </tr>
            <tr>
                <th>Prix_ht</th>
                <td>{{ produitDevi.prixht }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ produitDevi.quantite }}</td>
            </tr>
            <tr>
                <th>Remise_ht</th>
                <td>{{ produitDevi.remiseht }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produitdevis_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('produitdevis_edit', { 'id': produitDevi.id }) }}">Mettre à jour</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-danger" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
